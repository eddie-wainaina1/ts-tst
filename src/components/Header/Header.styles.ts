import React from "react"

const HeaderStyles: React.CSSProperties = {
    display: "flex",
    justifyContent: "space-evenly"
}

const LinkStyles: React.CSSProperties = {
    textDecoration: "none",
    color: "inherit"
}

export { HeaderStyles, LinkStyles }